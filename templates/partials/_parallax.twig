<!-- PARALLAX -->
  <div class="text-center text-md-right pb-5 parallax">
    <div class="container">
      <div class="row">
        <div class="p-5 mx-auto mx-md-0 ml-md-auto col-8 col-md-7">        
        {# HEADLINE #}
          {% if entry.homeHeadline %}
          <h3 class="h1 text-white">{{entry.homeHeadline}}</h3>
          {% else %}
          <h3 class="h1 text-white">{{global.homeHeadline}}</h3>
          {% endif %}

          {# subHeadline #}
          {% if entry.subHeadline %}
          <p class="mb-3 lead text-white">{{entry.subHeadline}}</p>
          {% else %}
          <p class="mb-3 lead text-white">{{global.subHeadline}}</p>
          {% endif %}

        </div>
      </div>
    </div>
  </div>